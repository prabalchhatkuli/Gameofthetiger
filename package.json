{
  "name": "gameoftiger-root",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd server && npm install && npm start",
    "heroku-postbuild": "npm run build && cd server && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
